1)
Başla
  İlk sayıyı al (sayı1)
  İkinci sayıyı al (sayı 2)
  Toplam = sayı1 + sayı2
  Yaz:" sayı1 ve sayı2'nin toplamı:" , toplam
Bitir


2)
Başla
    Toplam = 0
    Sayaç = 1
    Loop sayaç 100<= ise
        Toplam = Toplam + Sayaç
        Sayaç = Sayaç + 1
    DÖNGÜ bitir
    Yaz : Toplam
Bitir

3)
Başla
    Sayı = kullanıcıdan giriş al
    Eğer Sayı <= 1 ise:
        Yaz: sayı asal değildir
  Eğer sayı%1 = sayı ve sayı%sayı =0
  Yaz: sayı asaldır
Bitir
